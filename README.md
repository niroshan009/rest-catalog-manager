``` shell
docker run -d --name mypostgres -p 5432:5432 -e POSTGRES_PASSWORD=yourpassword postgres
```



```shell
--changeType=CREATE --tag=someTag

java -jar your-application.jar --changeType=CREATE --tag=someTag
```